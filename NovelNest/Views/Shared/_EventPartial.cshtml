@model EventServiceModel

@{
    CultureInfo.CurrentCulture = new System.Globalization.CultureInfo("en-EN");
}

<div class="card mb-4">
    <img class="card-img-top" src="@Model.ImageUrl" alt="Event Image">
    <div class="card-body">
        <h4 class="card-title"><strong>@Model.Topic</strong></h4>
        <p class="card-text"><strong>Location:</strong> @Model.Location</p>
        <p class="card-text">
            <strong>Start:</strong> @Model.StartDate.ToString("dd/MM/yyyy HH:mm")
        </p>
        <p class="card-text">
            <strong>End:</strong> @Model.EndDate.ToString("dd/MM/yyyy HH:mm")
        </p>
        @if (DateTime.Now < Model.StartDate)
        {
            <p class="card-text"><strong>Available Seats:</strong> @Model.Seats</p>
            <p class="card-text"><strong>Price:</strong> @Model.TicketPrice BGN</p>

            @if (Model.Seats < 10 && Model.Seats > 0)
            {
                <h5 class="card-text text-danger"><strong>LAST TICKETS AVAILABLE!</strong> Get yours before they run out!</h5>
            }
        }
    </div>
    <a asp-controller="Event" asp-action="Details" asp-route-information="@Model.GetInformation()" asp-route-id="@Model.Id" class="btn btn-warning mb-1 w-100 p-3 fw-bold">View more</a>

    @if (DateTime.Now < Model.StartDate && Model.Seats > 0)
    {
        <a asp-controller="Event" asp-action="BuyTicket"  asp-route-id="@Model.Id" class="btn btn-success mb-1 w-100 p-3 fw-bold">Get ticket</a>
    }
</div>